<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="itemViewModel"
            type="com.neat.viewmodel.ItemDetailsViewModel" />

        <import type="java.lang.Integer" />

    </data>

    <LinearLayout
        android:id="@+id/main_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:descendantFocusability="beforeDescendants"
        android:focusableInTouchMode="true"
        android:orientation="vertical">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <ScrollView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/white"
                android:transitionName="item_layout">

                <android.support.constraint.ConstraintLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    tools:context="com.neat.view.ItemDetailsActivity">


                    <ImageView
                        android:id="@+id/item_image"
                        android:layout_width="0dp"
                        android:layout_height="300dp"
                        android:scaleType="centerCrop"
                        android:transitionName="item_image"
                        app:itemImage="@{itemViewModel}"
                        app:layout_constraintLeft_toLeftOf="parent"
                        app:layout_constraintRight_toRightOf="parent"
                        app:layout_constraintTop_toTopOf="parent"
                        tools:src="@drawable/ic_bill_color" />

                    <TextView
                        android:id="@+id/item_title"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_marginEnd="16dp"
                        android:layout_marginStart="16dp"
                        android:layout_marginTop="@dimen/large_padding"
                        android:text="@{itemViewModel.name}"
                        android:textAppearance="@style/Text.Title"
                        android:transitionName="item_title"
                        app:layout_constraintLeft_toLeftOf="parent"
                        app:layout_constraintRight_toRightOf="parent"
                        app:layout_constraintTop_toBottomOf="@+id/item_image"
                        tools:text="Ensalada de guacamole, mozarella, tomate y pesto" />

                    <ImageView
                        android:id="@+id/item_icon"
                        android:layout_width="24dp"
                        android:layout_height="24dp"
                        android:layout_marginStart="16dp"
                        android:layout_marginTop="@dimen/large_padding"
                        android:tint="@color/text_secondary_dark"
                        app:itemIcon="@{itemViewModel.icon}"
                        app:layout_constraintLeft_toLeftOf="parent"
                        app:layout_constraintTop_toBottomOf="@+id/item_title" />

                    <TextView
                        android:id="@+id/item_price"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="@dimen/large_padding"
                        android:text="@{itemViewModel.priceText}"
                        android:textAppearance="@style/Text.Secondary"
                        app:layout_constraintBottom_toBottomOf="@+id/item_icon"
                        app:layout_constraintLeft_toRightOf="@+id/item_icon"
                        app:layout_constraintTop_toTopOf="@+id/item_icon"
                        tools:text="2,40€" />

                    <android.support.design.widget.TextInputLayout
                        android:id="@+id/note_edit_wrap"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:padding="@dimen/large_padding"
                        app:layout_constraintLeft_toLeftOf="parent"
                        app:layout_constraintRight_toRightOf="parent"
                        app:layout_constraintTop_toBottomOf="@+id/item_price">

                        <EditText
                            android:id="@+id/additional_instructions_edit"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:hint="@string/add_a_note"
                            android:inputType="textCapSentences"
                            android:text="@{itemViewModel.additionalInstructions}" />

                    </android.support.design.widget.TextInputLayout>

                    <ImageButton
                        android:id="@+id/count_down"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:enabled="@{itemViewModel.orderCount > 1}"
                        android:onClick="@{(view) -> itemViewModel.onCountDownClicked(view)}"
                        android:padding="@dimen/large_padding"
                        app:layout_constraintBottom_toBottomOf="@+id/count_field"
                        app:layout_constraintRight_toLeftOf="@+id/count_field"
                        app:layout_constraintTop_toTopOf="@+id/count_field"
                        app:srcCompat="@drawable/ic_arrow_drop_down_black_24dp" />

                    <TextView
                        android:id="@+id/count_field"
                        style="@style/Text.Display"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/large_padding"
                        android:ems="1"
                        android:gravity="center"
                        android:text="@{Integer.toString(itemViewModel.orderCount)}"
                        app:layout_constraintLeft_toLeftOf="parent"
                        app:layout_constraintRight_toRightOf="parent"
                        app:layout_constraintTop_toBottomOf="@+id/note_edit_wrap"
                        tools:text="1" />

                    <ImageButton
                        android:id="@+id/count_up"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:onClick="@{(view) -> itemViewModel.onCountUpClicked(view)}"
                        android:padding="@dimen/large_padding"
                        app:layout_constraintBottom_toBottomOf="@+id/count_field"
                        app:layout_constraintLeft_toRightOf="@+id/count_field"
                        app:layout_constraintTop_toTopOf="@+id/count_field"
                        app:srcCompat="@drawable/ic_arrow_drop_up_black_24dp" />

                </android.support.constraint.ConstraintLayout>
            </ScrollView>

            <Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="top"
                android:background="@drawable/bg_toolbar_shadow"
                android:elevation="1dp"
                android:fitsSystemWindows="true"/>

        </FrameLayout>

        <Button
            android:id="@+id/add_button"
            style="@style/Button.Accent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:onClick="@{(view) -> itemViewModel.onAddButtonClicked(view)}"
            android:padding="24dp"
            android:text="@string/add_to_pending_orders" />

    </LinearLayout>
</layout>